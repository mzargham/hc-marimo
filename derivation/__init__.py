"""Axiom-explicit SymPy derivation of the Homicidal Chauffeur optimal-play result.

One script per lemma (`lemma_01_*` ... `lemma_08_*`), chained by `run.py`. Every lemma
declares the axioms it invokes; the runner ends with an honest audit of what the whole
derivation leans on (posited modeling choices, inherited external theorems, and the one
regularity assumption that breaks on singular surfaces).

Plain SymPy. No sign/seal/hash.
"""
